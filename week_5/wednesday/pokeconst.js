/*const pokemon = {
    number: 50,
    name: "pikachu",
    type: "electric",
    moves: [right, left, up, down],
    hp: 120,
    takeDamage: function (hp) {
        this.for (hp = 0, hp < 100, hp-=10);
    }
    visitPokerCenter: function (hp) {
        this.for (hp = 0, hp < 100, hp+=10);
    }
    isFainted: function () {
        this.if (hp <= 100) console.log{}
        )
    }

}

// CONSTRUCTORS

function Car(color, make, model, horsepower) {
    this.color = color;
    this.make = make;
    this.model = model;
    this.horsepower = horsepower;
}*/

function Minion(name, numberOfEyes, color) {
    this.name = name;
    this.numberOfEyes = numberOfEyes;
    this.color = color;
    this.hungry = true;
    this.bored = true;
    this.cold = true;
    this.angry = true;
    
Minion.prototype.eatBanana = function() {
    if hungry {console.log ("May I have a banana please");}
Minion.prototype.performEvilTask = function() {
    if bored {console.log ("let's burn the building");}
Minion.prototype.changeNumberOfEyes = function() {
    if cold {console.log ("3 eyes");}
Minion.prototype.changeColors = function() {
    if angry {console.log ("purple");}

}