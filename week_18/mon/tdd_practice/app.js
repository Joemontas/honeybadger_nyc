function sortArray(array) {
    return array.sort();
};

function methodTotest (a,b,c) {
    return a + b + c;
}

function sinonSpy() {
    Date.now();
}

module.exports = {
    sortArray,
    methodToTest,
    sinonSpy
}